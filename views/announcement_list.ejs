<!DOCTYPE html>
<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://kit.fontawesome.com/e714a688f6.js" crossorigin="anonymous"></script>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://getbootstrap.com/docs/5.2/assets/css/docs.css" rel="stylesheet" />
  <link rel="stylesheet" type="text/css" href="/stylesheets/style.css" />
  <title>Announcement</title>
</head>

<body>
  <%- include('navigationBar.ejs') %>
    <main>
      <div class="container py-4">
        <header class="pb-3 mb-4 border-bottom text-center">
          <a href="/" class="d-flex justify-content-center align-items-center text-dark text-decoration-none">
            <i class="fa-solid fa-bullhorn fa-lg px-2"></i>
            <span class="fs-4">Library Announcement</span>
          </a>
        </header>
        <% if (announcement_list.length != 0) { %>
          <div class="p-5 mb-4 bg-light rounded-3 border ">
            <div class="container-fluid py-2">
              <h1 class="fw-bold text-center">
                <%= announcement_list[0].updatedAt.toLocaleString(undefined, {year: 'numeric' , month: '2-digit' ,
                  day: '2-digit' , weekday:"long", hour: '2-digit' , hour12: false, minute:'2-digit', second:'2-digit'})
                  %>
              </h1>
              <%- announcement_list[0].announcementContent %>
            </div>
          </div>
          <div class="card">
            <div class="card-header">
              <h4 class="card-title">
                Archived Announcements:
              </h4>
            </div>
            <div class="card-body">
              <ul class="list-group list-group-bordered">
                <li class="list-group-item">
                  <a href="/announcements">
                    <%= announcement_list[0].createdAt.toLocaleString(undefined, {year: 'numeric' , month: '2-digit' ,
                      day: '2-digit' , weekday:"long", hour: '2-digit' , hour12: false, minute:'2-digit',
                      second:'2-digit'}) %> (Latest)
                  </a>
                </li>

                <% for( let i=1; i < announcement_list.length; i++ ) { %>
                  <li class="list-group-item ">
                    <a href="/announcements/<%= announcement_list[i].id %>">
                      <%= announcement_list[i].createdAt.toLocaleString(undefined, {year: 'numeric' , month: '2-digit' ,
                        day: '2-digit' , weekday:"long", hour: '2-digit' , hour12: false, minute:'2-digit',
                        second:'2-digit'}) %>
                    </a>
                  </li>
                  <% } %>
              </ul>
            </div>
          </div>
          <% } else { %>
            <div class="container-fluid py-2">
              <h1 class="fw-bold text-center">
                No Announcement
              </h1>
            </div>
          <% } %>
      </div>
    </main>
</body>

</html>